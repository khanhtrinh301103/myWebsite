<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Seller Profile</title>
  <!-- Bootstrap CSS -->
  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/profile.css" rel="stylesheet">
  <link href="/css/seller-homepage.css" rel="stylesheet">
</head>
<body>
  <%- include('../partials/seller/header.ejs', { user: user, currentPath }) %>

  <div class="d-flex flex-column min-vh-100 profile-page">
    
    <div class="container mt-4 flex-grow-1">
      <div class="card profile-card mb-4">
        <div class="card-body d-flex flex-row">
          <div class="profile-photo-container">
            <img src="https://via.placeholder.com/150" alt="Profile Photo" class="profile-photo rounded-circle">
            <button class="btn btn-link change-photo-btn">Change Photo</button>
          </div>
          <div class="profile-info-container ml-4">
            <h2><%= user.fullName %></h2>
            <h6 class="text-muted"><%= user.role %></h6>
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>Phone Number:</strong> <%= user.phoneNumber %></p>
            <p><strong>Username:</strong> <%= user.username %></p>
            <a href="/auth/logout" class="btn btn-danger">Logout</a>
          </div>
        </div>
      </div>
    </div>

    <%- include('../partials/seller/footer.ejs') %>
  </div>

  <!-- Bootstrap JS and dependencies -->
  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
